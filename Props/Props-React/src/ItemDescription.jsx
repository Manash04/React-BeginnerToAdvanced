import React from "react";

function ItemDescription({ name, description }) {
  return (
    <div>
      <p>{name}</p>
      <p>
        <i>{descriptioon}</i>
      </p>
    </div>
  );
}

export default ItemDescription;
